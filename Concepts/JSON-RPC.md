JSON-RPC is a lightweight, stateless, remote procedural call (RPC) which uses JSON for data serialization.

It enables app to app communication, by allowing client to invoke a function directly on a server. 